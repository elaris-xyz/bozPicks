# Screenshot assets

The root `README.md` embeds these files. Save each screenshot with the **exact**
filename below into this folder (`docs/assets/`), then commit — the README will
render them automatically.

| Filename | Screenshot |
|---|---|
| `play.png` | `/play` — live Brazil–Argentina: Match Momentum, AI Pundit, Hi-Lo, win-probability gauge |
| `markets.png` | `/markets` — all 8 prop markets, order-flow rail, settlement pipeline |
| `receipt.png` | the Verifiable Resolution modal (resolving stat → Merkle root → proof path) |
| `agent.png` | `/agent` — Sharp-Move scorecard, Arena, In-Play Market Maker |
| `command-bridge.png` | the Command Bridge fixture/scenario picker |
| `vault.png` | the Game Vault modal (deposit → stake → cash out) |

PNG or JPG both work; keep them reasonably sized (≈1600px wide is plenty).
